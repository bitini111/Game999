include "config.path"
 
-- 启动的第一服务及参数，默认为snlua bootstrap。
-- 表示启动名为bootstrap的Lua服务，即service/bootstrap.lua。
bootstrap = "snlua bootstrap"
 
-- main script
start = "main"
 
-- logger
logger = nil
logservice = "logger"
logpath = "."
 
-- daemon
-- daemon = "skynet.pid"
